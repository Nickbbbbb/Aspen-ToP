# 打包逻辑说明

## 说明

本文档用于说明当前项目如何打包为 Windows 可执行文件，以及打包相关代码的整体逻辑。

当前项目打包后的产物是：

```text
dist\AspenToTop.exe
```

该可执行文件用于通过命令行完成：

- 单个 `.bkp` 文件转换
- 文件夹内多个 `.bkp` 文件批量转换

## 一、整体思路

打包逻辑本质上不是重新实现一套转换流程，而是将现有命令行程序连同运行时资源一起封装成一个 `.exe` 文件。

也就是说，打包解决的是：

- 如何把 Python 代码封装成可执行文件
- 如何把模板目录和外部加密工具一起带进去
- 如何让程序在源码运行和 exe 运行时都能找到正确的资源路径

转换本身的业务流程没有改变，仍然是：

```text
BKP
-> Aspen COM 提取数据
-> 标准化 Aspen JSON
-> ToP JSON
-> 调用外部加密工具
-> HSS
```

## 二、入口文件

打包入口文件是项目根目录下的：

```text
main.py
```

这个文件本身很薄，只负责把执行流程转发到：

```text
aspen_to_top/main.py
```

因此，无论是源码运行：

```bat
python main.py xxx.bkp
```

还是 exe 运行：

```bat
AspenToTop.exe xxx.bkp
```

最终走的都是同一套命令行逻辑。

## 三、真正的命令行主流程

核心命令行和编排逻辑位于：

```text
aspen_to_top/main.py
```

这个文件中有两个关键部分。

### 1. `AspenToTopConverter`

这个类负责把完整转换过程串起来，主要分为三步：

1. `_step1_extract`  
   使用 Aspen COM 打开 `.bkp` 文件，读取流程中的组分、模块、流股、物性方法、拓扑结构和坐标等信息。

2. `_step2_build_json`  
   将标准化 Aspen 数据映射为 ToP 所需的 JSON。

3. `_step3_encrypt`  
   调用外部 `hss_file_tool.py` 将 ToP JSON 加密为 `.hss` 文件。

### 2. `main()`

这个函数负责解析命令行参数，例如：

- 单文件转换
- 文件夹批量转换
- 只提取 JSON
- 只构建 ToP JSON
- 只加密为 HSS
- 解密 HSS

因此 exe 打包后，本质上仍然是执行这里的 `main()`。

## 四、为什么需要专门做运行时路径适配

源码运行和 exe 运行的目录结构并不一样。

### 1. 源码运行

源码运行时，程序可以直接从项目目录读取：

- `Template/`
- `export-test/`

例如：

```text
C:\Users\Administrator\Downloads\Aspen-ToP\Template
C:\Users\Administrator\Downloads\Aspen-ToP\export-test
```

### 2. exe 运行

exe 运行时，PyInstaller 会先把内置资源解包到一个临时目录中，然后程序在这个临时目录里读取资源。

这意味着打包后不能再简单依赖源码相对路径，否则会出现：

- 模板文件找不到
- 外部加密工具找不到

所以项目增加了统一路径模块：

```text
aspen_to_top/utils/runtime_paths.py
```

## 五、`runtime_paths.py` 的作用

文件位置：

```text
aspen_to_top/utils/runtime_paths.py
```

这个模块统一处理两类路径：

- 资源路径
- 输出路径

### 1. 判断是否为 exe 运行

核心判断逻辑：

```python
def is_frozen_app() -> bool:
    return bool(getattr(sys, "frozen", False))
```

PyInstaller 打包后的程序运行时会带有 `sys.frozen` 标记。

### 2. 资源根目录

资源根目录用于读取：

- `Template`
- `export-test`

逻辑如下：

- 如果是源码运行，资源根目录就是项目根目录
- 如果是 exe 运行，资源根目录就是 PyInstaller 的 `_MEIPASS` 临时解包目录

### 3. 工作目录根路径

工作目录根路径用于放输出文件，而不是读取模板。

逻辑如下：

- 源码运行时，输出仍放在项目根目录
- exe 运行时，输出放在 exe 所在目录

这样做的目的是让使用 exe 的同学可以很直观地在 exe 同目录下找到：

```text
output\xxx.hss
output\xxx.json
output\xxx.extracted.json
```

## 六、哪些模块使用了运行时路径

### 1. `aspen_to_top/main.py`

这个文件使用了：

- `TEMPLATE_DIR`
- `DEFAULT_OUTPUT_DIR`
- `WORKSPACE_ROOT`

这样转换器在初始化时就能根据当前运行环境自动决定：

- 模板从哪里读取
- 输出默认写到哪里

### 2. `aspen_to_top/converter/templates.py`

这个模块负责读取 `Template/` 目录中的所有 JSON 模板。

原先如果直接按源码相对路径读取，打包后就会失效。  
现在改成通过统一的 `TEMPLATE_DIR` 找模板，因此 exe 运行时也能正常读取：

- `final_template.json`
- `processNodes/*.json`
- `nodeProperties/*.json`
- `component/*.json`
- `componentPrivate/*.json`
- `methodPrivateList/*.json`

### 3. `aspen_to_top/encryption/hss_tool.py`

这个模块负责动态加载：

```text
export-test/hss_file_tool.py
```

原先如果直接从源码目录推导路径，exe 运行时会找不到外部工具。  
现在改成从统一的 `EXPORT_TOOL_DIR` 读取，因此 exe 运行时可以从解包目录加载这个文件。

## 七、为什么 `.spec` 文件很重要

打包配置文件位于：

```text
AspenToTop.spec
```

这个文件决定了 PyInstaller 在打包时需要把什么东西带进去。

### 1. 指定入口脚本

`.spec` 中指定：

```python
Analysis(["main.py"], ...)
```

表示打包时以根目录 `main.py` 作为程序入口。

### 2. 指定需要一并打包的数据目录

`.spec` 中定义了：

```python
datas = [
    (str(project_root / "Template"), "Template"),
    (str(project_root / "export-test"), "export-test"),
]
```

这一步非常关键。

因为项目运行时不仅依赖 Python 代码，还依赖：

- `Template/` 里的 ToP 模板
- `export-test/` 中的 `hss_file_tool.py`

如果不把这些资源一起打进去，exe 即使能启动，也会在运行到构建 JSON 或加密 HSS 时失败。

### 3. 指定隐藏依赖

`.spec` 中还定义了：

```python
hiddenimports = [
    "Crypto",
    "Crypto.Cipher",
    "Crypto.Cipher.AES",
    "Crypto.Util",
    "Crypto.Util.Padding",
    "pythoncom",
    "pywintypes",
    "win32timezone",
    "win32com",
    "win32com.client",
]
```

这是为了解决两类依赖不会被 PyInstaller 自动完整发现的问题。

#### 第一类：Aspen COM 相关

包括：

- `pythoncom`
- `pywintypes`
- `win32com`
- `win32com.client`

这些模块是 Aspen COM 调用必须依赖的运行库。

#### 第二类：加密工具相关

包括：

- `Crypto.Cipher.AES`
- `Crypto.Util.Padding`

你的 HSS 加密工具不是直接静态 import 的，而是通过 `importlib` 动态加载 `export-test/hss_file_tool.py`。  
这样会导致 PyInstaller 不能稳定识别这个外部工具内部还依赖了哪些模块，所以需要手动补充。

## 八、为什么 `Crypto` 需要手动处理

`export-test/hss_file_tool.py` 是外部工具，不作为标准包直接 import，而是通过：

```python
importlib.util.spec_from_file_location(...)
```

动态加载。

这意味着 PyInstaller 在分析项目依赖时，可能不知道这个外部文件内部还引用了：

- `Crypto.Cipher.AES`
- `Crypto.Util.Padding`

如果没有把这些模块显式声明到 `hiddenimports` 中，就会出现：

- exe 可以执行前两步
- 到 `Step 3: 加密为 HSS` 时失败

当前 `.spec` 文件已经将这部分依赖补齐。

## 九、打包脚本 `build_exe.bat`

为了方便打包，项目根目录中增加了：

```text
build_exe.bat
```

这个脚本做的事情依次是：

1. 切换到项目根目录
2. 安装或升级打包依赖
3. 安装或升级加密依赖
4. 清理旧的 `build/` 和 `dist/`
5. 调用 PyInstaller 执行打包

脚本中会安装：

- `setuptools`
- `pyinstaller`
- `pycryptodome`

其中：

- `setuptools` 用于补齐某些 PyInstaller 运行依赖
- `pyinstaller` 用于真正打包
- `pycryptodome` 用于支持 `Crypto.Cipher.AES`

### 打包命令

脚本最终执行的是：

```bat
pyinstaller --clean AspenToTop.spec
```

其中 `--clean` 用于清理 PyInstaller 缓存，避免旧分析结果影响新版本打包。

## 十、exe 启动后的执行路径

打包完成后，执行：

```bat
AspenToTop.exe xxx.bkp
```

整体流程如下：

```text
AspenToTop.exe
-> main.py
-> aspen_to_top/main.py 中的 main()
-> AspenToTopConverter.convert()
-> Step 1 提取 Aspen 数据
-> Step 2 构建 ToP JSON
-> Step 3 调用外部工具加密为 HSS
```

所以，打包没有新增一套业务逻辑，只是把当前已有的命令行程序封装成了一个可以直接双击或命令行调用的可执行文件。

## 十一、当前打包能力的边界

当前打包后的可执行文件是：

```text
Windows .exe
```

因此只能在 Windows 环境中使用。

除了文件格式本身是 Windows 可执行文件以外，更重要的原因是：

### 1. Aspen 读取依赖 COM

项目读取 `.bkp` 文件的核心方式是 Aspen Plus COM 接口，而 COM 是 Windows 平台机制。

因此：

- `BKP -> Aspen 提取数据`

这一段基本只能在：

- Windows
- 已安装 Aspen Plus
- COM 可正常调用

的环境中运行。

### 2. JSON 构建和加密更容易跨平台

从逻辑上看：

- `标准化 Aspen JSON -> ToP JSON -> HSS`

这段更接近纯 Python 数据处理，理论上更容易独立做成跨平台能力。

但当前这个完整 exe 仍然是面向 Windows 的。

## 十二、当前打包方案的价值

这套打包方案的意义主要在于：

1. 使用者不需要手动执行多个 Python 脚本
2. 模板和外部工具被统一封装
3. 单文件和批量转换都可以直接通过命令行完成
4. 源码运行和 exe 运行共用一套业务逻辑，维护成本更低

## 十三、相关文件

当前和打包直接相关的文件如下：

```text
main.py
aspen_to_top/main.py
aspen_to_top/utils/runtime_paths.py
aspen_to_top/converter/templates.py
aspen_to_top/encryption/hss_tool.py
AspenToTop.spec
build_exe.bat
docs/可执行文件打包说明.md
```

## 十四、常见问题

### 1. exe 能否在 Linux 或 macOS 运行

不能。

当前产物是 Windows `.exe`，且核心读取能力依赖 Aspen COM。

### 2. exe 能否脱离 Aspen 使用

不能完成完整的 `.bkp -> .hss` 转换。

如果没有 Aspen COM 环境，第一步读取 `.bkp` 就无法执行。

### 3. 为什么要把 `Template` 和 `export-test` 一起打包

因为它们不是辅助文件，而是运行所必需的资源：

- `Template` 决定 ToP JSON 的模板结构
- `export-test/hss_file_tool.py` 负责最终加密为 `.hss`

### 4. 为什么 exe 已经能生成 JSON，却在加密时报错

通常是因为打包时没有把 `Crypto` 相关依赖正确带进去。  
当前方案已经通过 `.spec` 中的 `hiddenimports` 修复了这个问题。
